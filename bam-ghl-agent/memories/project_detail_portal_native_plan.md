# DETAIL Miami → fully portal-native (GHL = placeholder until Twilio)

**Decision (2026-07-07, Zoran):** get DETAIL Miami onto V2 and make the portal the source of truth for **everything** - contacts, pipeline, opportunities, automations, agents, KPIs, email, calendars, members/billing. **GHL survives only as the interim SMS/voice line until Twilio lands, then is fully retired.**

Plan artifact: https://claude.ai/code/artifact/19cefb74-1d3b-4d45-b3e2-6c364025a2bd

## The key architectural fact (why the order is what it is)
GHL messaging is **contact-centric**: outbound needs a real GHL `contactId` (`api/ghl/send-message.js:337`), inbound webhooks only fire for GHL contacts. The built portal-native contact path (`api/_contacts.js:53` `resolveOrMintPortalContact`) **makes NO GHL call** - it mints a uuid used as both `contacts.id` and `ghl_contact_id`. So portal-native contacts + GHL messaging are **incompatible** without net-new dual-write code.

**Resolution = don't fight it.** While GHL is the phone line, contacts stay `contact_provider='ghl'` (GHL master, portal mirrors via `ghl_contacts`). Academy never logs into GHL anyway (V1.5), so it already feels fully portal-native. **The contacts flip to `contact_provider='portal'` is the LAST step, gated on the Twilio cutover** (once `messaging_provider='twilio'`, GHL is no longer the phone line, so contacts can leave safely). Zero throwaway bridge code.

## Phases
- **Phase 0 - Get on V2 (foundation, first):** EIN+address (gate) → publish offers (9 exist, 0 published) → Stripe Matcher (0 pricing) → import members (0) → link members↔GHL contacts + pipeline+entry-points↔offers → flip `v2_access`. See [[project_v2_onboarding_model]].
- **Phase 1 - spines off GHL (all proven on GTA, none need Twilio):** KPIs (`kpi_events`, easy) · pipeline (`pipeline_provider='portal'`, easy) · automations (nudge/follow-up + entry routing - draft GHL workflows OFF first) · agents (booking/confirm brain for Detail, sandbox→live) · email (Resend, needs DNS MX on detail-mia.com) · calendars (Luka's core spine, per-academy schedule build). See [[project_contacts_offghl]] [[project_pipeline_offghl]] [[project_kpis_offghl]] [[project_email_spine]] [[project_calendars_offghl]] [[project_automation_agent_roadmap]].
- **Phase 2 - Twilio cutover (keystone):** master Twilio + TrustHub + A2P (BAM-wide, TrustHub is the multi-week long pole - START EARLY, runs parallel to 0+1) → port Detail's LC number, watcher auto-flips `messaging_provider='twilio'` → then flip `contact_provider='portal'` → GHL retired. See [[project_twilio_messaging_spine]].

## Detail Miami state (2026-07-07, id 4708a68d)
GHL connected, Stripe Connect connected, Meta wired, KPI config set, all Blueprint done-flags set, 5 team logins, **6 GHL pipelines incl. TRAINING PIPELINE** (Interested→Responded→Schedule Trial→Done Trial, matches GTA). Currently: `v15_access=true`, all providers = `ghl`. Gaps: **0 members, 0 pricing rows, 9 offers/0 published, 2 entry points/0 offer-linked, EIN+address null.**

## Progress log
- **2026-07-07:** Set `clients.address` (16410 NW 54th Ave, Miami Lakes, FL 33014) + `time_zone`=America/New_York. EIN still pending (gates offer publish/buildout only).
- **2026-07-07:** Wired both calendar entry points (Free Trial Elementary + MS/HS) → offer_id=Training (`7d82f15e`), pipeline='TRAINING PIPELINE', stage='Schedule Trial' (+ stripped em-dash labels). Live offer set = 5 drafts (Training/ADAPT/Rentals/Camps/AEL); Unlimited/2x/1x archived (belong as price tiers inside Training via Stripe Matcher).
- **Webhook check:** `api/website/miami-book.js` books into Detail's 2 calendars (hardcoded loc RBnlVgmXNMbFpgFGPGcv). Appointment routing = app-level `AppointmentCreate` → `api/ghl/inbound-webhook.js:136` (Detail passes v15 gate), resolves stage via `_store.js:65` regex `/(schedul|book).*trial/i` → matches "Schedule Trial" ✅, moves the OPEN opp there + exits drips. ⚠️ TWO caveats: (1) confirm the FC LeadConnector app is subscribed to `AppointmentCreate` (separate event from the confirmed InboundMessage sub); (2) the webhook only MOVES an existing open opp - a cold booking with no prior opp won't create a card, confirm the site creates the lead/opp first.

- **2026-07-07 test booking (webhook check):** BLOCKED upstream. Both Detail GHL free-trial calendars (290AH0…/HPQ2Ws…) return **0 open slots over 45 days** (`/api/website/availability`) and a direct book via `miami-book.js` was rejected by GHL: *"slot you have selected is no longer available."* → **the GHL calendars have no bookable availability/open-hours configured.** So the AppointmentCreate webhook couldn't be confirmed live (no appointment can be created). Note: `miami-book.js` upserts the contact BEFORE booking, so a `ZZTEST` contact (tags miami-lead/miami-free-trial-booked) was created in Detail's GHL and then **deleted as cleanup**. NEXT for calendar-website sync: either (a) configure the GHL calendars' hours/availability for the interim, or (b) jump to Luka's portal-native calendar spine (`booking_provider='portal'`, `schedule_slots`), which the plan wants anyway. Then re-run the same test to confirm the webhook + confirm the FC app is subscribed to AppointmentCreate.

- **2026-07-07 calendar = Path B chosen (Zoran):** go portal-native on Luka's spine, not fix GHL calendar. The whole stack is BUILT + proven on GTA (see [[project_calendars_offghl]]). Replicate via portal proxy `/api/runtime/schedule/*` (templates.ts create, template.ts patch/delete, generate-slots.ts) using the temp-staff-auth pattern from `scripts/extend-gta-slots.mjs` (mint temp staff → sign in → call → cleanup). Steps: create Detail "Training" program + slot templates (per group, recurrence WEEKLY:xx HH:MM-HH:MM, capacity, TZ America/New_York) → generate-slots +60d → set entry_points.bookable_program_id + align "Group N" label↔template-name matching (availability.js portal branch filters slots by /group\s*\d+/ from the entry_point label; Detail's labels are Elementary/MS-HS so needs a group token or code tweak) → flip `clients.booking_provider='portal'` → monthly slot-coverage extend (Routine like GTA's) → re-run the test booking to confirm webhook + card→Schedule Trial + KPI. **BLOCKER: need Detail's real training schedule** (days/times/capacity per group Elementary + MS/HS) - their GHL calendars are EMPTY so it can't be extracted; Zoran/Mike must provide it.

- **2026-07-07 Path B dependency chain (KEY FINDING):** portal-native calendar is DOWNSTREAM of the member-mgmt core, not standalone. A slot template requires a `bookable_programs` row (tenant_id); `resolveBookableProgramId` errors "is required" with none. `bookable_programs` are created by **offers-sync** (`api/runtime/offers-sync.ts`), which HARD-REQUIRES **confirmed `pricing_catalog` rows** ("run the Stripe Matcher first"). Detail has **0 programs, 0 confirmed pricing, 0 offers synced**. So the calendar chain is: Training offer → **Stripe Matcher** (confirmed pricing) → offers-sync → bookable_program → slot template → generate-slots → flip `booking_provider='portal'`. Can't build the schedule until offers+pricing are done. Mike's MS/HS schedule (Mon+Wed 6-8pm, **cap 25 per Zoran**, Elementary skipped) is captured to replicate once the program exists.
- **2026-07-07 auth/env reality:** runtime endpoints (`/api/runtime/*`) need a real staff JWT (`getStaffContext`, no secret bypass). Minting one needs the Supabase **service-role key**, which is NOT in this environment (only `.env.example`). So the proper run path is THROUGH THE AUTHENTICATED PORTAL UI (Stripe Matcher button, schedule editor), not a bash script. The schedule editor UI (capacity+hours inputs wired to the runtime template API for portal-native academies) is greenlit to BUILD (Zoran 2026-07-07) - today the v15 settings drawer is read-only for portal-native (`calendars_offghl` note).

## External blockers (start early)
Twilio TrustHub approval (gates Phase 2), Luka's per-academy calendar spine (gates calendars), Resend DNS on detail-mia.com (gates email).
