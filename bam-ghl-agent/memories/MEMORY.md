# Memories — bam-ghl-agent

Project notes for the BAM GHL Agent / portal work. Read the relevant file when its topic comes up.

- [Sales crew — the MODEL (design)](project_sales_crew_model.md) — 2026-06-25 PLANNING/spec (not built). The full multi-agent sales system: spine 👻 Ghosted→💔 Lead Nurture→📅 Booking→✅ Confirm→🎯 Closing→🎉 Member. Agents = live convo (Hawkeye approve each msg); automations = one-way nudges (approve sequence once). Terminal: 🚫 Unqualified = ONLY dead end (removed from pipeline); everyone else marked Lost at any stage + ghosted-out → 💔 Lead Nurture. Ghosted aggressive→Nurture sparse (email+text via Resend). Confirm+Closing each have a fixed initial automation that turns OFF when the lead replies → agent goes live. Shared follow-up scheduler replaces per-agent nudge queue. Brain examples organized by action. GTA qualified = near Oakville + 9+; mirror GTA's GHL pipeline. Visual: `docs/sales-crew-model.html`. See [[project_sales_crew_guardrails]] + [[project_confirm_agent]].
- [Sales crew — multi-bot guardrails](project_sales_crew_guardrails.md) — 2026-06-25 Zoran's non-negotiables: notify on ALL inbound · pipeline glows per active bot (automation stages = dashed outline, agent = solid glow) · Hawkeye for every bot · turn a bot off globally + per-convo/per-card · first-come-first-serve on Hawkeye cards · client can see inside every automation (counts total + per step → people list → contact drawer). Agents approve each msg; automations approve the recipe.

- [Confirm agent (Scheduled-Trial stage)](project_confirm_agent.md) — 2026-06-25: 2nd sales agent. Brain is now agent-aware (`assemblePrompt(overrides, agent)`, `AGENT_SPECS={booking,confirm}`, shared facts). Confirms a booked lead is coming + helps them get there; "can't make it" → HANDS OFF to booking agent (note to `agent_contact_notes` + bounce stage Scheduled-Trial→Responded). Live: `api/agent-confirm.js` + `agent_confirm_replies` table (migration `20260625001106`, ⚠️maybe not applied) + own cron + `confirm_agent_mode` (default OFF). ⚠️ no frontend yet; booking agent untouched (byte-identical).

- [V1.5 Onboarding Tracker](project_v15_onboarding_tracker.md) — **living board** for getting every training academy onto V1.5 (text leads + use pipelines). Definition of done (core vs polish), per-academy status, per-location connect process. DETAIL Miami = first one live.
- [V1.5 Rollout — infra + resume](project_v15_rollout.md) — 2026-06-22: session infra for moving all academies to V1.5. Built `/api/ghl/all-pipelines` (public report — academy rows + stage→arrow→stage + saving notes via `pipeline_notes`; 28 academies/106 pipelines live), `/api/contacts?action=auto-map-athletes` (idempotent — maps full-name athlete field per v15 academy; re-run after flipping), FC **app-level `InboundMessage` webhook** → `/api/ghl/inbound-webhook` (no per-academy setup, confirmed; delete GTA's old "agent trigger" workflow). Agency one-click mint BLOCKED (FC=Sub-Account app) → per-account connect. Only 4 on v15; REMAINING = flip real academies (skip test rows), re-run auto-map, owner logins. `/v15continue` resumes.

- [Quiet Lead → Send to Ghosted](project_followup_forcing_function.md) — 2026-06-24: REPLACES the "always schedule a follow-up" forcing function. Responded leads quiet ≥24h (from our last msg) get a `kind:'ghost'` card in Hawkeye's new 👻 Went quiet tab (detector is LLM-free, queues into `agent_ready_replies`). Staff click **👻 Send to Ghosted** = `agent-approvals confirm-ghost` (enroll `offers.data.ghosted_workflow` + move to Interested via new `_stage.js interestedStage`) or ✕ → Lost/Abandon/Skip + optional training note. KILLED: auto nudge-drafting, the mandatory 2-step modal, `draft-one`, `_PL_FU_PENDING`. Badge 🔴 Needs action = has ghost card. `agent_followups` table kept (legacy drain). `FollowupsPanel.jsx` not mirrored. BAM GTA / V2-agent, V1 untouched. **UPDATE 2026-06-24 (later):** ghost is now the ≥24h fallback for EVERY Responded lead - direction-agnostic, iterates the full roster (not top-100 convos), reply engine bounded to <24h, send-guard checks LIVE GHL. See note for the final model.
- [V2 Sales Board + Home Dashboard](project_v2_sales_board.md) — 2026-06-24: the V2 Training-Pipeline board (Responded cockpit + inline-Hawkeye drawer, agent suggestion in the chat thread, per-stage card rules: Interested red flag, Scheduled-Trial phase partition + squiggle dividers, Done-Trial lead-last glow, post-trial form gated to started trials), duplicate detector (`_PL_DUPS` phone/athlete → `_plResolveDup`), and the new V2 Home dashboard (`renderHomeV2`, 4 animated count tiles). All gated `_plIsV2()`; V1.5 = plain GHL mirror. Post-trial first-message rework. Home tab un-hidden for V2.

- [Session pickup — KPI + Sales (2026-06-10)](project_session_pickup_kpi_sales.md) — **READ FIRST when resuming.** What's live (KPI dashboard, journey board, Sales tab wizard, member docs, clients status bar) + open follow-ups.

- [Per-member documents (waivers etc)](project_member_documents.md) — signed waivers/media/medical stored per member in a PRIVATE `member-files` bucket + `member_files` table (signed URLs), shown in the Members popup → Documents. Mirrors `offer_files` but private. Vercel intake-form auto-drop is the follow-up.
- [bam-portal deploy (how to actually ship)](project_bam_portal_deploy.md) — ✅ merge to main → Vercel auto-deploys (re-verified 2026-06-11). ⚠️ `vercel redeploy` still rebuilds OLD source — if forcing a manual deploy, clean `vercel deploy --prod` from the REPO ROOT with `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` env vars.

- [Prod schema dump](project_prod_schema_dump.md) — `node bam-portal/scripts/migration/dump-prod-schema.mjs <sbp_token>` snapshots prod tables/RLS/policies to `bam-portal/supabase/snapshots/prod-schema.sql`; re-run + git diff = see prod drift. Read-only.
- [Supabase local replay and migration cleanup](project_supabase_local_replay.md) — **READ before migrations/seeds/repair/fetch.** Points to `bam-portal/supabase/README.md`, the source of truth for historical backfills, local seed ordering, linked repair requirements, and `fetch --linked` limits.
- [BAM GHL North Star](project_bam_ghl_north_star.md) — dual client+staff portal (chat, ad spend, tickets), not just an autonomous agent
- [BAM GHL Agent Files](project_bam_ghl_agent_files.md) — local + git + Vercel file locations and workflow
- [Client Portal Flow](project_client_portal_flow.md) — onboarding.html → client row, client-portal.html shows tickets, live on Vercel
- [Action Items + ONBOARDING](project_action_items.md) — **the client onboarding flow lives here.** Shared to-do list AND the 17-step Onboarding checklist (subway-map UI, collapsible, hides when done; SM/Cam/Ximena booking steps; staff-only trigger-buildout + review gate). **Has a "📍 ONBOARDING STATE — read first" block at the top + open items.**
- [Support Ticket System](project_support_ticket_system.md) — full ticket pipeline + Systems portal status
- [BAM Onboarding Checkpoints](project_bam_onboarding_checkpoints.md) — current 14-step sheet checklist, baseline for Supabase redesign
- [Slack Onboarding Automation](project_slack_onboarding_automation.md) — auto-create Slack channels for new clients
- [iOS Push via PWA](project_ios_push_pwa.md) — deferred PWA + Web Push plan for iPhone client notifications
- [Menu Items Complete](project_menu_items_complete.md) — All portal menu items run through setup-menu-item phases; no pending work
- [Training Onboarding Flow](project_training_onboarding.md) — 3-step standalone flow, all DB questions inserted
- [Tasks System Pending Mike](project_tasks_pending_mike.md) — task unification on hold until Mike gives preferences
- [Questions Database Schema](supabase_questions_db.md) — full schema, enums, valid Places Asked, insert rules, **UUID→Question lookup pattern** for ticket field labels
- [Local Dev Workflow](project_local_dev_workflow.md) — `npm run dev` hits prod Supabase; use test_business client (71d01c0f-...) for safe testing + cleanup query
- [Marketing + Content Workflow](project_marketing_content_flow.md) — two-stage ticket flow (client → content team → marketing team), `content_tickets` + `marketing_tickets` schema, single `api/marketing.js`, full UI map on both portals. **2026-06-18: Cam flow (priority/deadline/SLA/sort/assigned-SM), folder-block content drop + `raw_files.folder`, asset-type auto-derive, video thumbnails, 🎨 brand card. ⚠️ Slack DMs blocked on missing `im:write` scope.**
- [Organic Content](project_organic_content.md) — **2026-06-18: 2nd content pipeline.** `content_tickets.channel` ('ads'|'organic') + `clients.organic_content` gate. Client Marketing tab → Ads | Organic split; organic = Request → Status (in-progress/review) → Creative Bank; review = Approve/Request-changes. Staff "Send for client review". Per-client staff toggle.
- [Client Action Thread](project_client_action_thread.md) — multi-round chat between staff & client on a ticket: `tickets.messages` jsonb, state machine, API actions, UI mapping
- [Client Action Notifications (TODO)](project_client_action_notifications_todo.md) — deferred: Slack/email/SMS notifications when staff requests action or client responds
- [Client Portal Auth](project_client_auth.md) — email + password login, RLS-scoped queries, invite/recovery machinery. NOTE: cardinality is now MANY users per client — see Multi-User note.
- [Pre-Launch Checklist](project_pre_launch_checklist.md) — everything intentionally deferred during dev: SMTP for prod email, branded templates, signed URLs, notifications, audit trail, etc. Walk through 🔴 items before first real client.
- [Channel Dashboard](project_channel_dashboard.md) — internal-only dashboard for the 6-week basketball acquisition test (Cole/Mike/Zoran only). Tables `bam_channel_snapshots` + `bam_channel_settings`, ingest folded into `api/stripe/overview.js?section=channel-ingest`. Manual funnel edit until Meta API lands.
- [Meta API Integration](project_meta_api_integration.md) — HYBRID: staff-side OR client-side OAuth, `client_meta_tokens` preferred when present, falls back to `staff_meta_tokens`. Sample-data fallback on Marketing tab when neither wired.
- [Ad Performance Dashboard](project_ad_performance_dashboard.md) — 2026-06-06: automates Ximena's by-hand Meta→sheet KPI report. `GET /api/marketing?resource=meta-report` (per-campaign, per-month, one Meta call). Client portal Marketing tab: Simple/Advanced toggle, conversion funnel, rule-based insights, industry-benchmark + per-client-goal colouring. v1 Meta-only (ROAS=v2). Needs `marketing_goals.sql` run + Notion MKT-/onboarding-data sync (TODO).
- [Public Onboarding URL](project_public_onboarding.md) — `/onboarding.html` is the shareable signup link for new clients + testers. Lands them in client portal with sample marketing data + optional "Connect Meta" CTA.
- [Clients Supabase Consolidation](project_clients_supabase_consolidation.md) — 2026-05-17 migration: `clients.name` → `business_name`, added `scaling_manager_id` FK + `scaling_manager` role, backfilled 15 rows + created Out Work / Alex Twin / Alex Silva. UI still uses `name` alias.
- [2026-05-17 Polish Session](project_session_2026_05_17_polish.md) — Round-1+2 audits, 3 critical security fixes, Resend password reset (Supabase template was broken), self-serve forgot-password, admin feedback widget + Feedback tab, FullControl design system applied to client portal, font playground.
- [Client Portal First-Login Tour](project_client_portal_tour.md) — 2026-05-18 ship: 8-step spotlight tour on first login, demo data injection, `?preview=tour` URL, `?action=complete-onboarding` API.
- [Team Page Wired](project_team_page_wired.md) — 2026-05-18: Team tab was rendering hardcoded SAMPLE_STAFF + modals hit nonexistent /api/staff URLs (with deceptive 404=success fallback). Now reads from supabase + all 3 modal actions go through api/clients.js (update-staff + reset-staff-password actions added).
- [Client Onboarding Method](project_client_onboarding_method.md) — 2026-05-19: added clients.onboarding_method ('call' | 'send_link') + clients.call_completed_at. Status pill on Clients list/detail derives from method + completion (Call pending / Pending link accept / Live). Known gap: send_link "Live" doesn't flip status to active.
- [Client Portal Mobile Layout](project_client_portal_mobile.md) — 2026-05-20: phone layout pass for client-portal.html. Fixed bottom tab bar replaces the hidden sidebar; comprehensive ≤768px stylesheet (bottom-sheet modals, full-height chat, dvh, safe-area, 16px inputs). Desktop untouched.
- [App Store / Native App Launch](project_app_store_launch.md) — 2026-05-20: getting the client portal onto the iOS App Store + Google Play. PWA shipped; Capacitor wrapper built; server.url verified; push notifications wired (wrapper-complete — captures device tokens to Supabase `device_tokens`). Next: run device-tokens.sql, compile on a Mac, submit. **Active — read this to continue the app work.**
- [Member Management → Client Portal](project_member_management_portal.md) — 2026-05-20: BAM GTA member-management system incorporated into the client portal as a client-side "Members" feature. Phases 1+2 DONE — tab LIVE for BAM GTA (51 athletes migrated, `MEMBER_MGMT_ENABLED=true`). Stripe model = Connect. Next = Phase 3 (billing actions). Resume via `/member-management-continue`.
- [Multi-User Client Portal Access](project_multi_user_portal.md) — 2026-05-20: many logins per academy via `client_users` join table (owner + invited teammates). SHIPPED end to end — DB + RLS, `invite/revoke-team-member` API, Team section in client portal (+ mobile), Team tab in staff portal. Also fixed a pre-existing wide-open staff RLS hole. Only the Notion write-up is outstanding.
- [Onboarding Reloaded](project_onboarding_reloaded.md) — 2026-05-21: reworked client onboarding flow `onboarding-reloaded.html` — 6 sections, conditional offer/team record builders, staff+location tables, Supabase-synced (table not yet created). 51 jsdom checks pass. **Resume with `/onboarding`.**
- [Resources Library](project_resources_library.md) — 2026-05-22: global admin-published library. Tables `resources` / `resource_files` / `resource_categories` + public `resources` bucket; RLS gated by `is_admin_staff()`. Staff CRUD in `bam-portal/src/views/ResourcesView.jsx` (admin-only nav). Client view in `client-portal.html` — tile grid + search + hash-routed detail page (`#resource=<id>`) with inline preview for image/video/PDF.
- [Offer Concept Visual](project_offer_concept_visual.md) — 2026-05-23: Zoran's hub-and-spoke sketch of the Offer concept (sales · schedule · price · value · tracking · onboarding). Saved as `bam-portal/public/offer-concept.png`. Will surface inside BB > Offers card as a "what is an offer?" explainer once the editor ships. Treat as a sealed asset — don't redesign without sign-off.
- [Offer Architecture](project_offer_architecture.md) — Canonical doc for the 6 offer types, Team's per-team specialness, offers/offer_teams/offer_files schema, the dual offer-scope/team-scope field renderer, file-upload storage layout. Read before touching the BB Offers wizard. Update on new offer types, field-type additions, or schema changes.
- [V2 Onboarding Model](project_v2_onboarding_model.md) — **Source of truth** for V1 vs V2, `clients.v2_access` (renamed from `onboarding_in_progress` 2026-05-27), the 5 BB cards + done-state derivation per section, the tracker pill, the welcome-Slack flow, the first-login tour, the auto-resend-invite cron. **Must be updated in the same commit as any change to onboarding behavior — see the upkeep section at the bottom of the note.** BAM GTA is now V2.
- [Per-staff permissions + Preview-as](project_staff_permissions.md) — 2026-06-19/20: owner/BAM-staff control each teammate's TABS, pipeline STAGES, KPI CATEGORIES (`client_users.allowed_tabs/allowed_stages/allowed_kpis`, subtractive, NULL=all); add-without-invite placeholder + ✉ Invite; `hide_from_team` hides BAM staff; **Preview-as** (account menu + mobile More); default-to-owned-academy; Home hidden for V2. Fil + Adrian seeded in GTA. Live-agent link = `/sandbox`.
- [V2 Sales/Inbox/mobile UI pass](project_v2_sales_inbox_ui.md) — 2026-06-18/20: Summer-special on all cards → auto-move to Interested; Won/Lost/Abandoned remove from board; missed-trial `ghl_workflow` offer field → post-trial fires it; email OFF (SMS only); inbox boxed list + activity-msgs hidden + pipeline-filter=live-only + owner-only Talk-to-BAM + full thread; CAC + live/paused KPIs; V2 mobile bar (Members·Sales·Inbox·KPIs·More); Stripe links open the Stripe app on mobile.
- [Agent Sessions](project_agent_sessions.md) — 2026-05-28: Zoran-only review of Cam/Coleman/Mike/Rosano Claude Code sessions. `/showtime` + `/byebye` skills push transcript to `agent_sessions` table; server generates dual technical+visual summaries via Claude; UI inside Zoran's Feedback tab as a "Feedback / Agent sessions" pill switcher. Needs `AGENT_SESSION_INGEST_SECRET` env var in Vercel + per-user `~/.claude/showtime-config.json`.
- [API Auth Hardening](project_api_auth_hardening.md) — closed unauthenticated endpoints exposing GHL CRM PII (`api/ghl.js`) + Stripe financials (`api/stripe/overview.js`); fail-closed GHL webhook. `/api/ghl?action=*` is now **staff-only** — callers must send the token. Lists still-deferred items (ai/search, training, OAuth state secrets).
- [Staff Nav Persistence](project_staff_nav_persistence.md) — staff portal remembers the active tab across reloads via `localStorage` (`bam_nav`), with a role-gated fallback to Inbox. Fixes "I lose my place jumping between tabs/windows."
- [Sentry Observability](project_sentry_observability.md) — Complete PRD/MVP for production-only Sentry: admin-only read-only App Errors tab, top 5 production issues by frequency, no DB writes, separate from Feedback, instrument staff web + client portal/mobile WebView + Vercel API.
- [Onboarding Wizard — parked](project_onboarding_wizard_parked.md) — 2026-05-29: ideal 3-step setup wizard (Sign up → Connect Stripe + auto-import subs → Connect GHL → tour). PARKED until BAM GTA's GHL OAuth flow is verified end-to-end. ~1.5 days build when unparked. Trigger: Zoran confirms "test SMS arrived + Inbox shows conversations."
- [Pause Lifecycle](project_pause_lifecycle.md) — 2026-05-30: single-mode pause (start_date + end_date). Pause length always extends the next-charge date (`trial_end = max(now, period_end) + length`). Future-scheduled pauses queued via `cancellations.activated_at=null`. Hourly cron (`cron-process-scheduled-pauses` at :15) activates due pauses and auto-recovers ended ones. All edge cases (past-due, payment_failed, cancelling, re-pause, 730-day cap) handled.
- [Next Session Pickup](project_next_session_pickup.md) — **READ FIRST.** 2026-05-30 (session end): GHL OAuth LIVE for BAM GTA; Kun Liu/Ryan + John Fu backfilled. Next = test the 6 PATCH actions end-to-end starting with Cancel Ryan (clears Sergio's ticket #4). Has the full state pointers + all the GHL gotchas (white-label "ghl" path block, trim() pattern, scope incompatibilities, etc) so future-you doesn't redo them.
- [Stripe "sub not app-created" gotcha](project_stripe_app_created_subs.md) — 2026-06-01: Stripe Connect rejects portal pause/cancel/change/refund on subs NOT created by the app ("can't make changes on a subscription that was not created by your application"). Workaround = handle billing by hand in Stripe + DB-only pause (status flip + active cancellations row for cron recovery). Will affect the 6-PATCH-action testing for legacy/backfilled members. Did this for Knowl Beharie. See [[project_coachiq_integration]] for why (CoachIQ owns the subs).
- [CoachIQ integration](project_coachiq_integration.md) — **STRATEGIC: sell FullControl to academies already on CoachIQ.** ⭐ **ARCHITECTURE (DECIDED 2026-06-05):** FullControl OWNS Stripe billing (creates subs + the pause/cancel/change buttons); CoachIQ stays ACADEMY-RUN for credits/scheduling; the link = the academy pastes the portal's sub_id into CoachIQ (their existing workflow). **BAM builds NO credit bridge** → scales across academies (CoachIQ can watch an external portal-owned sub_id — Zoran confirmed). DEPRECATED (proven but now unused): credit webhook bridge (`api/coachiq.js`, PR #54), per-amount Add-Credits automations, credit model/expiry — academy handles credits in CoachIQ. IN SCOPE: portal create-sub (`api/coachiq-billing.js`, PR #52) + existing billing buttons + Track A funnel (portal payment) + Track B migration (recreate subs portal-owned → academy re-links sub_ids). Background: BAM GTA Stripe = STANDARD connect (portal can only manage subs IT created → migration needed); subs split CoachIQ 68 / GHL 9 / manual 23.
- [Engineering Guide + Safe Session Workflow](project_engineering_guide_and_session_workflow.md) — `docs/portal-engineering-guide.md` (auto-loaded by `/showtime`) is the canonical "know-everything + build-safely" doc. `/showtime` primes the session, `/byebye` generates a test script before send. API hardening it spawned: canonical `api/_roles.js` (role sets), `api/_env.js` `requireEnv`, `GET /api/health`. Audit calls: `client_users` is live (not dead); `board_items`+`content_*` deprecated (drop after 2026-07-01, backup first); `client_meta_tokens` fallback kept on purpose; eslint doesn't cover `api/` so `node --check`+build+import tests are the real gate.
- [BAM GTA billing hygiene](project_bamgta_billing_hygiene.md) — 2026-06-05: data issues found while scoping the migration. Some members are `live` in our DB but their Stripe sub is dead/incomplete (e.g. Ebaad Wahid); several PREPAID via one-time charges that never SAVED a card (e.g. Luke/Jim Newton — $854.28, card detached) so their renewals WILL FAIL (Jul 8 for Luke). The 4 with no reusable card: Ebaad, Krishay, Luke, Syed Faiz. Link type matters: dead sub → subscription checkout; alive-no-card → card-collection (setup) link. DB athlete name ≠ Stripe parent name (cleanup needed).
- [Offer ⇄ Stripe ⇄ CoachIQ price mapping](project_offer_price_mapping.md) — 2026-06-07: per-academy feature. In BB → Offers → Pricing, each offer-price (plan×term) ties to a Stripe canonical + legacy prices + one CoachIQ product. "Match with AI" (re-runnable, review-first) reconciles live subs → offer-prices using amount/name/interval/metadata/origin + asks owner. Legacy = recognize old subs only (new signups use canonical). CoachIQ product ids harvested from Stripe sub metadata.productId (CoachIQ API can't list products). Storage = extend pricing_catalog (offer_id, offer_price_key, coachiq_product_id, match_*). Becomes THE source the funnel reads. Multi-tenant. Build phases: data model → AI matcher → BB Pricing UI → funnel rewire. See docs/api-data-map.html for what each API can collect (GTA example).
- [The Pricing Sorter / UI name "Stripe Matcher" (onboarding wizard)](project_pricing_sorter_wizard.md) — 2026-06-09: guided wizard from Onboarding Action Items (after Offers ✓), on BOTH staff + client portals. 2026-06-11: UI renamed "Stripe Matcher" + BB Pricing card got a clickable Stripe→Match→Import→Cleanup progress strip (done-flags via /api/members `sorter`). Step 1: visual boxes+arrows match of offer prices ↔ Stripe subs/products (reuses /api/offers/match-prices); no-match → AI recommends + CREATES the Stripe price on approval, explained plainly. Step 2: upload arbitrary client CSV → AI column mapping → checkmark confirm → populate a SEPARATE per-client members table. Step 3: cleanup connectivity issues. 2026-06-15: Cleanup table got a **Next payment** column + `api/sorter/fix-payment.js` (deterministic fix + Claude haiku sanity-check) + blocked-sub replace fallback + auto-restore-on-reopen (#348). Builds on [[project_offer_price_mapping]].
- [Website Leads (client-site forms → portal → GHL)](project_website_leads.md) — 2026-06-11: save-first lead capture + Entry Points routing layer. Every client-site form submission writes website_leads FIRST (rows = form fills), then GHL: upsert contact, tags (website-inquiry + per-form + entry_points row tags), NOTE with message text (no conversation provider yet so thread stays empty), opportunity in the pipeline/stage mapped on the entry_points row. Entry Points rail + Set Up wizard live in client portal Pipelines view. CORS allow-list = clients.allowed_domains. Wired: by-any-means + bam-gta. GHL automations trigger on TAG not form-submitted.
- [Parent App DB Boundary](../docs/luka-parent-app-db-plans.md) — 2026-06-12: CHECK BEFORE SCHEMA CHANGES. Luka's reserved tables (identity/billing/scheduling for fc-mobile parent app), shared tables needing a sync (clients, members, pricing_catalog, locations), and the RLS rule: new tables must use is_staff()/my_client_ids(), never plain authenticated — parents will hold real JWTs in this project.
- [Automation + Sales Agent Roadmap](project_automation_agent_roadmap.md) — 2026-06-11: replace GHL automations portal-native. Phases: ① inbound-message webhook spine ② nudge engine (sms ghosted; BLOCKED on Zoran message copy/timings; owner-flag ledger ENGINE→AGENT→HUMAN) ③ agent co-pilot (books real slots, schedule_followup, confidence-gated escalation queue) ④ auto + retire GHL workflows. Training: prompt draft → sandbox → live-with-escalations (lessons = answer + why) → trusted trainers (Mike+) → all academies. Lessons born local, earn global via promotion queue. Metric: agent convo → booked-trial rate per brain version. GHL API cannot read contact workflow membership.
- [Client Agent Training + Brain Follow-ups](project_client_agent_training.md) — 2026-06-20: (1) follow-ups now 3 editable Brain `goal` sections (followup_triggers/timing/exclusions) in `prompt-structure.js` (PR #588). (2) Client-portal agent training (PR #592): `client_users.can_train_agent` opt-in grant (Zoran+Fil on BAM GTA); `api/agent-train.js` local-only (lessons born academy, only location/offer brain editable); AI classifier routes general sales-craft → `agent_lessons.promotion_status='pending'` → staff approval queue in AgentTrainingView (approve flips scope→general). New `Train Agent` nav (desktop tab + mobile More). ⚠️ global scope still just a flag — no cross-academy sink yet. (3) Follow-up engine (PR #595): `agent_followups` table + `api/agent-followups.js` detect (cron */30, drafts next nudge for quiet leads from the brain) + work (cron 1m, sends APPROVED due rows) + cancel-on-reply in inbound-webhook; staff `⏰ Follow-ups` tab (approve-each, nothing sends unapproved); gated by `ghl_kpi_config.followup_engine_enabled` (BAM GTA on). ⚠️ disable GHL follow-up workflow steps before approving (double-text). (4) 2026-06-22: read-time Responded gate (Hawkeye list-ready/list endpoints filter stale cards via `_stage.js` respondedContactIdSet/cached/peek, fail-open) + Quiet hours 8am–9:30pm Toronto (`_quiet.js`) on ALL send paths — out-of-window sends HOLD via `agent_ready_replies.send_after` (migration 20260622210000, applied) and flush next morning; API returns `{deferred,send_after}`.
- [Feedback → Action system](project_feedback_to_action.md) — 2026-06-14: turn "Ask Zoran" feedback into shipped work with minimal founder activation energy. **Phase 1 SHIPPED** = daily cron `GET /api/clients?action=cron-feedback-digest` → Claude triages open `portal_feedback` (action + S/M/L effort) → Slack digest to `FEEDBACK_SLACK_CHANNEL`. Phases 2–3 (one-tap build → auto-drafted PRs to approve) planned.

- [Our Ads (internal campaigns view)](project_our_ads_view.md) — 2026-06-14: staff-only "Our Ads" tab showing BAM's OWN ad campaigns. Reuses the exported per-client `MarketingTab` (account+campaign picker + dashboard) pointed at a dedicated internal `clients` entry. Email-gated to Zoran/Mike/Coleman; internal entry chosen via `VITE_INTERNAL_ADS_CLIENT_ID` (one-time setup). Our campaign picks are independent of any client's — never changes what clients see.

- [V1.5 Tier + Contacts Tab](project_v15_tier.md) — 2026-06-16: V1.5 = academy never logs into GHL; the portal is their full CRM, GHL synced live. Staff set tier via a V1/V1.5/V2 segmented control (clients.v15_access + v2_access, mutually exclusive). First V1.5 surface = the **Contacts tab** (client portal, gated by applyV15NavState): search parent/athlete name + phone + email, filter by tag, reading the new **ghl_contacts mirror** (cron-sync-contacts upserts it for v15 academies; athlete_name resolved from a mapped GHL custom field). Setup maps the athlete-name custom field (`/api/contacts?action=custom-fields` → has-data + suggestion → clients.v15_config). More V1.5 tabs coming.
- [Per-Academy Asset Library](project_asset_library.md) — 2026-06-15: one image library per academy in the client portal ("Assets" left-nav tab). Table `client_assets` (+ `client-assets` public bucket) — each image tagged to an offer/staff/location or left as a brand asset. RLS mirrors `offers`. Phase 1 DONE (tab: upload/grid/filter/tag/remove via `openAssetsView` in client-portal.html). Pending: `/api/website/assets` + wiring offer/staff/location uploads into it + sites pulling by tag + migrating GTA's git images & the Claude Design photo library in. Design system = tokens in git (`bam-client-sites`); images = per-academy in Supabase.
- [Website Enrollment Funnel](project_website_enrollment_funnel.md) — 2026-06-15: parent join→pay→sign funnel on the academy's OWN site (`bam-client-sites` `clients/bam-gta/enroll.html`, direct link only). 3 offer-driven steps: intake (typed fields) → Price-Matched plan → scroll-gated agreement + draw-signature → Stripe Payment Element. Backend `api/website/offer.js` + `api/website/checkout.js` (public, CORS via `clients.allowed_domains`; reuses onboarding/checkout Stripe flow; price by `offer_price_key`, never trusts client amount). Signed PDF (pdf-lib, sample text) → `member-files` + a `member_files` waiver row (auto-shows in member popup) + `members.agreement_pdf_path` flag. Member goes live + GHL convert/tag via the existing invoice.paid webhook. Welcome video moved Onboarding→Sales (training). GTA offer 52a6285c published, plans left archived → shows only live "Summer Unlimited".
- [Sales page + Communications tab](project_sales_comms.md) — 2026-06-14: Pipelines→Sales board (entry-points arrow rail, Parent/Athlete cards, rich drawer w/ journey timeline + full lead detail). Post-trial form (show-up/fit/trainer/notes → moves to Done Trial, queues signup text GATED). Done-trial = Won(stub)/Lost(live) not post-trial. Pricing Sorter can match an existing Stripe price OR create. COMMS TAB: Phase 1 done (tag selectors on Sales=lead/Onboarding=client in offers.data, comms-config API, inbox enriched w/ trainer+channel, contact_trainers table). Phase 2 PENDING = the tabbed comms UI (trainer tabs + Business → Leads/Clients → thread, reply SMS+email, inline assign trainer+tag). Trainer from post-trial form + inline-assignable. lead-wins/untagged→lead.
- [AI pricing-change assistant](project_ai_pricing_changes.md) — future: AI calcs future payments from text + system data (price changes are a nightmare)
- [Member Import + CoachIQ + Summer billing — RESUME](project_member_import_coachiq_state.md) — 2026-06-19 pickup: what's shipped, 4 remaining builds, Summer billing spec, env/gotchas
- [Import/Cleanup UX standards + backlog](project_import_cleanup_ux.md) — 2026-06-20: STANDING RULE no native alert()/confirm()/prompt() in import/cleanup flows → styled modals (model on _sorterFixSuccess). Pending: styled popups everywhere, 'Not a member'→cancellation popup if active sub, 'Not a member' into TO-DO column, adjustable cancellation date.
- [Owner/staff SMS notifications](project_owner_notifications.md) — 2026-06-23/24: per-academy SMS to the teammates an academy picks (Blueprint → Staff → 🔔), from the academy's own GHL number, for 8 events. Helper `api/_notify-owners.js`; `clients.notification_prefs` jsonb (event → `client_users.id`); phones from `client_users.phone`. ⚠️ calendar bookings need the FC app subscribed to `AppointmentCreate`. GTA fully wired to Zoran.
- [Scheduling-app switch (CoachIQ off for GTA)](project_coachiq_integration.md) — 2026-06-24: per-academy `clients.scheduling_app` ('coachiq'|'none') segmented control on the staff Overview tab; `update-fields` syncs `coachiq_enabled` so all existing CoachIQ gates cascade. GTA='none' (CoachIQ off everywhere). GTA enroll site CoachIQ step → Join WhatsApp group. (Section in the CoachIQ note.)
- [Portal additions (late June 2026)](project_portal_additions_jun2026.md) — 2026-06-23/24 digest: onboarding feedback form (hard-block → client_notes), contact tags (Contacts+Inbox), stripe-on-any-contact (phone match + cache), member-mgmt modals, KPI Trends graphs, mandatory offer fields + buildout gate (EIN/address), V1.5 pipelines = plain GHL mirror, teammate phone, portal_feedback client_id+resolve-text. Lists all new tables/columns + the stale-duplicate-folder gotcha.
