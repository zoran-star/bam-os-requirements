# Auto-build smoke test

Labeling any GitHub issue with `auto-implement` triggers the Claude Code Action defined in `.github/workflows/auto-implement.yml`.
The action reads the issue body, implements the described change, and opens a pull request — no manual intervention needed.
This file was created automatically to confirm the pipeline works end to end.
